import React from 'react';
import type { Step } from '../types';

interface Props {
  currentStep: Step;
}

/**
 * Компонент индикатора прогресса
 * 
 * Подумайте:
 * 1. Как визуально показать связь между шагами?
 * 2. Как отобразить состояние каждого шага (активный/завершён/будущий)?
 * 3. Должен ли индикатор быть интерактивным?
 */
export const ProgressBar: React.FC<Props> = ({ currentStep }) => {
  // Изучите: почему мы выносим массив шагов за пределы компонента?
  const steps: Step[] = ['personal', 'work', 'additional'];
  
  /**
   * Подумайте:
   * 1. Как рассчитать процент заполнения?
   * 2. Нужно ли анимировать переходы?
   * 3. Как сделать компонент доступным для скринридеров?
   */
  return (
    <div className="progress-bar">
      {/* 
        Изучите: 
        1. Как работает семантическая верстка?
        2. Что такое ARIA-атрибуты и зачем они нужны?
        3. Как сделать компонент адаптивным?
      */}
    </div>
  );
};
