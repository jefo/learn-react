import React from 'react';

interface Props {
  step: number;
  onNext: () => void;
  onPrev: () => void;
}

/**
 * 📝 Шаг формы
 * 
 * Подумайте:
 * 
 * 1. Состояние формы:
 *    - Как форма узнает какие поля показывать?
 *    - Где хранить данные каждого шага?
 *    - Как избежать потери данных?
 * 
 * 2. Валидация:
 *    - Когда проверять данные?
 *    - Как показывать ошибки?
 *    - Что делать с частично валидными данными?
 * 
 * 3. Навигация:
 *    - Когда разрешать переход?
 *    - Как обрабатывать возврат?
 *    - Что показывать пользователю?
 * 
 * 💡 Подсказка:
 * Представьте, что вы проводите собеседование.
 * Как бы вы структурировали вопросы и помогали
 * кандидату двигаться по ним?
 */
export const FormStep: React.FC<Props> = ({
  step,
  onNext,
  onPrev
}) => {
  // Реализуйте компонент
  return null;
};
